name: CI

on:
  push:
  pull_request:
    branches: [main]

# Cancel any in-flight run for the same ref when a new commit lands.
# Saves CI minutes on rapid pushes; the latest result is the only one that matters.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: build / lint / test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          # Pin to whatever go.mod declares so CI never drifts from local.
          go-version-file: go.mod
          cache: true

      - name: Build
        run: make build

      - name: Lint
        run: make lint

      - name: Test
        run: make test
